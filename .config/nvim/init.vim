" I don't like this new convention, just source my existing .nvimrc plz
source ~/.nvimrc

" Entrypoint to Lua configuration.
" (user/config.lua requires other Lua files as needed.)
lua require('user.config')
