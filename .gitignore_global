tags
tags.lock
tags.temp
tags.temp.tmp
**/.lsp/sqlite*.db
# clj-kondo seems to make identical cache and .cache folders for some reason???
# could be related to clojure-lsp's integration with clj-kondo
**/.clj-kondo/cache/
**/.clj-kondo/.cache/
# These lines shouldn't be necessary, but fzf is supposed to be excluding them
# and is failing to do so. It seems like maybe fzf isn't parsing my gitignore
# correctly?
.clj-kondo/cache/
.clj-kondo/.cache/
